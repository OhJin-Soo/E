3 + 4^2 # 산술 연산
log(10) # 로그 계산
exp(2) # 지수 계산
sqrt(10) # 제곱근 계산
1:15 # 연속된 수의 표현
sum(1:15) # 연속된 수의 합계
rnorm(12) # 정규분포에서 12개 숫자를 임의 생성
mean(rnorm(12)) # 평균의 계산
sample(20) # 표본 20개 생성
max(sample(20)) # 최대값 계산